# PID - Product Idea Document

> Tab Assistant 产品想法文档

---

## 功能一：标签快照与一键恢复 ⭐⭐⭐⭐⭐

### 痛点分析
- 浏览器崩溃或重启后，精心整理的标签分组全没了
- 更换设备或重装 Chrome 时，工作环境无法恢复
- 经常需要临时保存某个工作状态

### 功能描述

#### 📸 手动创建快照
- 保存当前所有标签 URL + 分组状态 + 窗口布局
- 支持为快照添加自定义名称和备注
- 快照列表显示创建时间、标签数量、窗口数量

#### ⏰ 自动快照
- 每小时/每天自动备份（用户可选）
- 保留最近 N 个自动快照（如最近 7 天）
- 自动快照标记为「自动」，手动快照标记为「手动」

#### 🔄 一键恢复
- 从快照列表选择，恢复到指定工作状态
- 恢复前提示：确认操作（避免误操作）
- 部分恢复：只恢复特定分组的标签（高级选项）

#### 🗑️ 快照管理
- 查看快照详情：包含所有标签 URL 和分组信息
- 删除快照（支持批量删除）
- 导出快照为 JSON 文件（备份到本地）

### 使用场景
1. **工作前拍快照**：开始工作时创建快照，结束时一键恢复初始清理状态
2. **项目切换**：不同项目保存不同快照，快速切换工作环境
3. **浏览器崩溃恢复**：自动快照 + 手动快照结合，崩溃后快速恢复

### 优先级
**P0 - 高优先级**

### 实现要点
- 使用 `chrome.storage.local` 存储快照数据
- 快照数据结构：
  ```json
  {
    "id": "uuid",
    "name": "工作日快照",
    "type": "manual|auto",
    "createdAt": 1234567890,
    "windowCount": 3,
    "tabCount": 45,
    "data": {
      "windows": [...],
      "groups": [...]
    }
  }
  ```
- 快照限制：最多保存 50 个快照，超过后自动删除最早的

---

## 功能二：智能分组助手 / 规则推荐引擎 ⭐⭐⭐⭐

### 痛点分析
- 新用户不知道如何配置规则，需要手动一个个域名去填
- 用户经常打开的网站没有规则，每次都手动分组很麻烦
- 正则表达式对普通用户有学习门槛

### 功能描述

#### 🤖 分析现有标签
- 统计标签频率：哪些域名/URL 模式最常出现
- 访问时长分析：哪些分组占用最多的时间
- URL 特征提取：识别 URL 中的共同模式（如 `/dashboard`、`/admin`）

#### 💡 自动推荐规则
- 基于分析结果，生成智能分组建议
- 推荐规则包含：分组名称、匹配类型（域名/正则）、匹配内容、建议颜色
- 显示置信度：基于标签数量和频率

#### ✅ 一键采纳
- 用户点击确认，自动添加到规则列表
- 支持批量采纳（全选推荐规则）
- 采纳后自动重新分组当前标签

#### 📊 规则可视化
- 用图表展示哪些网站占用最多标签（饼图/条形图）
- 显示分组统计：每个分组的标签数、占比
- 未分组标签占比分析

### 使用场景
1. **首次安装引导**：分析当前标签，生成 5-10 条推荐规则
2. **周期性分析**：每周/每月提醒用户是否有新规则建议
3. **主动提醒**：对于频繁访问但未配置规则的网站，主动提醒添加

### 优先级
**P1 - 中优先级**

### 实现要点
- 使用 `chrome.tabs.query()` 获取当前所有标签
- 统计算法：按域名聚合，计算频率和占比
- 正则模式提取：识别 URL 中的共同路径模式
- UI 位置：在「规则」页面新增「智能推荐」Tab

---

## 功能三：标签页搜索与快速跳转 ⭐⭐⭐⭐

### 痛点分析
- 标签多了（100+）时，找某个特定页面很痛苦
- 浏览器的 Ctrl+Tab 只能顺序切换，不支持搜索
- 只记得页面标题里的关键词，但不记得在哪个分组

### 功能描述

#### 🔍 全局搜索
- 按标题/URL/域名搜索所有标签
- 实时搜索：输入即显示匹配结果
- 支持模糊匹配和正则表达式（高级选项）

#### ⌨️ 快捷键唤起
- 自定义快捷键（默认 `Cmd+Shift+T`）
- 快捷键唤起搜索框，支持键盘操作
- ESC 关闭搜索框

#### 🎯 精准跳转
- 选中结果后直接聚焦到该标签
- 同时展开该标签所在分组
- 跨窗口搜索：可以跳转到其他窗口的标签

#### 🔖 搜索历史
- 记住最近搜索的关键词
- 搜索框支持下拉选择历史关键词
- 支持清除搜索历史

### 使用场景
1. **快速定位**：记得页面标题中的某个关键词，快速定位
2. **跨窗口切换**：在多个窗口中查找特定页面
3. **模糊记忆**：只记得域名但不记得具体 URL，快速搜索

### 优先级
**P2 - 低优先级**
（与浏览器自带功能有重叠，用户体验提升有限）

### 实现要点
- 使用 `chrome.commands` API 注册快捷键
- 搜索框 UI：类似 VS Code 的命令面板，悬浮在页面顶部
- 键盘导航：`↑↓` 选择结果，`Enter` 确认，`ESC` 取消

---

## 功能四：跨设备同步与云端备份 ⭐⭐⭐⭐⭐

### 痛点分析
- 家里和公司的电脑规则不统一，每次都要重新配置
- 重装 Chrome 后，规则和分组配置全没了
- 多人协作时，希望共享某个项目的标签分组规则

### 功能描述

#### ☁️ 云端同步
- 规则配置自动同步到所有登录的 Chrome 设备
- 使用 `chrome.storage.sync` API（原生支持跨设备）
- 域名别名、设置项同样支持同步

#### 📦 自动备份
- 配置变更时自动备份到云端
- 支持备份到 GitHub Gist（用户授权后）
- 保留最近 N 个备份版本（如最近 10 次）

#### 🔄 导入/导出
- 从云备份一键恢复配置
- 导出配置为 JSON 文件（本地备份）
- 导入配置文件（支持版本检测）

#### 👥 规则分享
- 生成规则配置链接（分享给同事）
- 链接格式：`https://tab-assistant.io/s?config=xxx`
- 导入规则时预览，用户确认后才应用

### 使用场景
1. **多设备统一**：家里的 Mac 和公司的 Windows 使用相同规则
2. **重装恢复**：重装 Chrome 后一键恢复所有配置
3. **团队协作**：团队成员共享项目分组规则，统一工作方式

### 优先级
**P0 - 高优先级**
（解决核心痛点，实现成本低）

### 实现要点
- 使用 `chrome.storage.sync` 同步规则和设置
- `chrome.storage.sync` 限制：每个配置项最大 8KB，总共 100KB
- 大型规则配置使用 `chrome.storage.local` + 手动同步
- GitHub Gist 备份需要 OAuth 2.0 授权

---

## 功能优先级排序

| 优先级 | 功能 | 理由 |
|--------|------|------|
| **P0** | 跨设备同步 | 解决多设备使用痛点，实现成本低 |
| **P0** | 标签快照 | 解决浏览器崩溃问题，核心刚需 |
| **P1** | 智能分组 | 降低使用门槛，提升新用户体验 |
| **P2** | 标签搜索 | 与浏览器功能重叠，提升有限 |

---

## 技术栈参考

| 功能 | 使用 API / 技术 |
|------|-----------------|
| 标签快照 | `chrome.storage.local`, `chrome.tabs.query` |
| 智能分组 | `chrome.tabs.query`, 统计算法, 正则提取 |
| 标签搜索 | `chrome.commands`, `chrome.tabs.query`, React Portal |
| 跨设备同步 | `chrome.storage.sync`, GitHub API (OAuth) |

---

## 版本规划

| 版本 | 功能 | 预计时间 |
|------|------|----------|
| v1.3.0 | 跨设备同步 | 2 周 |
| v1.4.0 | 标签快照 | 2 周 |
| v1.5.0 | 智能分组 | 3 周 |
| v1.6.0 | 标签搜索 | 1 周 |

---

*文档创建时间：2026-01-31*
*创建者：阿呆 (ai: aidai) 🤔*
